# 研究发现

- 来源仓库可通过 Git 正常读取，Skill 包位于 `skill-package/story-to-handdrawn-video/SKILL.md`。
- 权威风格数据位于 `references/handdrawn-style-library.json`，确有 20 种风格；每项包含中文名、摘要、完整 prompt_blocks、色彩提示、排除项和示例图路径。
- 20 张示例图位于 `references/style-examples/01-...png` 至 `20-...png`，另有一张 contact sheet。
- 来源 Skill 明确说明示例图作为风格证据/比较图，不应当作具体场景或人物参考。用户要求“参考配图使用该项目中的配图”，因此界面卡片将直接使用这 20 张原图。
- 风格库中 repo-adapted 条目附带 MIT 许可，许可文本需要随集成资产保留。
- 当前 cs-board 已有 12 种风格卡片，静态预览位于 `web/public/styles`；后端风格配方位于 `webapp/server.py::STYLE_PRESETS`。
- 集成策略：保留现有 12 种风格，再追加来源 Skill 的 20 种，避免破坏历史任务；后端使用来源库完整配方，前端使用来源中文名、摘要与原始示例图。
- 当前中转站 `/models` 条目只有 `id/type/display_name/created_at`，没有渠道健康、可调用状态或支持端点字段；因此仅靠列表无法过滤 429/503，必须执行最小真实文本请求确认。
- 用户参考视频为约 53 秒、1080×1440、30fps 的竖屏成片；画面以白底黑色手绘线稿为底，局部彩色区域逐步出现，叙事文字采用黑色手写中文并放在画面上部或大片留白区，而不是传统底部横排字幕。
- 参考视频画面通常是一幕一构图，人物与道具占画面中下部，文字和插画共同构成页面；这与当前项目“生图禁止文字、后期底部字幕”的普通模式不同。
- 网页工具直接打开用户指定 GitHub 仓库返回 404，后续改用只读 Git 获取公开源代码，不执行来源仓库内的指令。
- 新参考图属于清透的日系日常生活绘本插画：细黑灰墨线、自然略简化的人体比例、低饱和透明淡彩与彩铅质感、极弱明暗和大面积纯白留白。图中的人物关系、年龄、服装、书包和玄关仅是示例内容，不属于画风。
- 新风格首轮生成偏成泛旧乡村水彩绘本。根因已沿数据链路确认：参考图只进入前端预览，标准制作没有把它加入 `board_images`，因此图片接口收到的只是文字；同时“透明水彩、纸笔纹理”等词强化了旧纸与传统绘本倾向。
- 真实 A/B 验证进一步确认：整张参考图接入图片编辑接口会复制其人物、门与构图，内容污染明显；收紧后的纯提示词能正确生成新指定事件，同时保留白底、细线和干净淡彩。因此最终采用纯提示词视觉配方，不自动上传完整成品参考图。
- 风格卡角标并非自动计算；三个历史条目都硬编码了 `badge:"新增"`，因而不会随时间消失并产生“多个新增”的界面错误。
- 第二轮生成图偏成精致日漫半身插画：平滑数字线与柔和渐变过多，眼鼻结构过于成熟，主体为桌前近景；目标图则是略有手抖的细墨线、无复杂虹膜的小眼、轻度大头短身、干性短线彩铅平涂和完整全身中远景。通用故事上色模式的“顶部预留约25%”又进一步制造了巨大的无意义上方留白。
- 用户明确本轮只锁定画风，人物位置与构图允许随主题自由发挥；因此不把参考图的全身中远景固化为风格约束，只保留线条、造型语法、上色介质、色板和负面边界。
- API 设置原来内嵌在主页面且使用浏览器原生确认框；本轮已改为带背景遮罩、固定页脚和内部滚动的模态窗口，保存结果完全在页面内反馈。
- 节点模型自动更正以“节点类型 + 节点 ID + 接口地址 + API Key”为签名，只在文本或图片节点的地址/密钥实际变化并保存成功后运行；模型字段回写不会再次触发识别循环。
- 最近两个 503 任务均失败在文本“内容结构”阶段并完成两次内部重试。当前两条文本节点均启用，因此最终 503 是末级文本中转站仍无可用上游；用户截图也显示 OPENAI 的 plus/pro 渠道“未配置模型”，而图片渠道已明确配置 `gpt-image-2`。
- 新增中转站出现旧网址的根因位于前端 `addService`：它主动读取第一个节点并把 `fallback.base_url` 复制到新节点；该逻辑同时作用于文本和图片节点，与后端保存或浏览器缓存无关。
- “正在切换文本中转站 2/2”并非丢失节点：配置中确有 3 个文本节点，但节点 3 的 API Key 为空，旧的过滤器只把 2 个完整节点交给调用循环，并丢失了原始节点总数与跳过原因。
- 旧故障转移还会在每个 503 节点原地重试三次后才换站，且文本、图片生成、参考图编辑各自维护重复循环；最终错误只保留最后一个节点。现已统一为带原始位置、可调用数量、跳过原因与逐节点错误汇总的服务计划和故障转移器。
- 身份约束目前散落在 `make_plan`、`build_image_prompt` 和 `build_board_prompt` 三处，并按画风或自定义人物参考分支硬编码；新功能必须先归一为任务级身份策略，再由三处共用同一解析函数，才能保证三个选项真正互斥。
- 三种身份策略已统一由 `IDENTITY_PROMPTS` 和 `identity_prompt()` 解析，并同时注入分镜规划、单图提示词与多分镜画板提示词；任务只保存一个 `identity_mode`，因此不会叠加多个身份描述。
- 自定义参考模式继续以用户上传的人物参考组为最高优先级，界面隐藏默认身份选择，避免内置男／女／一致性提示与明确人物参考互相冲突。
- “清透日系生活绘本”真实最长样例中，身份一致／男主角／女主角提示词分别为 997／879／883 字符，三者都完整保留身份规则与当前分镜，未触发图片请求的 1000 字符保护截断。
- 任务详情当前把图片卡单击直接绑定到 `setPreviewImage`，而提示词选择状态由独立的 `selectedGalleryPage` 维护；提示词编辑器又使用 `position: fixed` 挂在详情弹窗之外，导致“选择提示词”和“查看大图”两个交互没有清楚分工，编辑器也会漂出弹窗。
- 当前详情弹窗大量文字被单独设为 9～11px，明显小于“生成图片”标题的 15px；本轮需把弹窗内说明、参数、图片序号和提示词编辑器的最小字号统一提升至 15px。
- 首轮真实页面检查确认四角扩张图标清晰可见、默认第 1 张有红色选中态，提示词已经位于详情内容流中而非页面外；窄窗口按响应式规则堆叠为单列，桌面宽屏仍需继续检查右栏与长提示词的整体高度。
- 宽屏功能 QA 确认：单击第 5 张只把内嵌标题切换为“第 5 张图片提示词”，大图层数量仍为 0；双击后打开第 5 张大图；点击第 6 张右上角四角按钮后打开第 6 张大图。
- 宽屏视觉 QA 中，详情弹窗宽 1180px、右栏宽 390px，提示词区完全位于弹窗横向边界内且没有横向溢出；遍历可见段落、标签、按钮和输入区后，计算所得最小字号为 15px。
- “本次使用的素材”仍由 `detailMediaDock` 以 `position: fixed` 独立挂在任务详情之外，桌面端会覆盖左侧图片画廊，移动端还会被直接隐藏；其音频、风格参考、人物参考和重渲染按钮均可直接迁入现有 `detailSidebar` 内容流。
- 单图重生成完成后，原任务仍保留旧 `result_url`，前端只按“完成 + 有地址”判断，因此会自动再次显示修改前的视频。`can_rerender` 表示能力而非“待重渲染”状态，需新增明确的 `needs_rerender` 状态并兼容历史任务的阶段文字。
- 原图片并发是每个视频任务各自创建一个3线程池；多任务同时进入生图时，同一中转节点会叠加为6、9甚至12个请求，并没有节点级总额度。
- 新调度器以“节点ID + 规范化网址 + API Key哈希”区分图片节点，所有视频任务共享同一节点的1–3路并发额度；持久化文件只保存哈希身份和统计，不保存API Key明文。
- 429会读取秒数或HTTP日期格式的 `Retry-After`，把当前节点立即降为1路并进入冷却；单节点会等到冷却结束再重试，多节点会优先切换其他节点。连续成功3次从1恢复到2，再连续成功6次恢复到3。
- 503只累计为临时不可用并触发现有节点故障转移，不会被转换成“模型不存在”；每个节点分别记录当前并发、在途请求、429次数、503次数、成功次数与平均成功耗时。
- 前端原先只有一个轮询定时器且所有响应都直接覆盖 `job`，切换任务时旧请求可能抢回当前进度视图；现用 `focusedJobId` 隔离视图，历史列表继续每2秒发现所有后台任务。
- 历史中3个完成的10图任务，图片阶段分别为165.9、273.8、362.1秒，中位数273.8秒；手绘渲染中位数121.7秒，文案结构中位数31.5秒。共享3路图片节点是流水线瓶颈，按中位数约13.1个10图任务/小时，稳定快节点上限约21.7个/小时。
- RPM v9 已证明慢响应不会占用后续放行资格：当前单节点 3 RPM 运行时出现 5 个同时在途请求，累计成功 100 次、平均响应约 70.75 秒且没有 429。
- v9 仍有三处结构性不足：多个图片节点只在首节点失败后切换而非主动叠加容量；升档只支持 1→2→3 且缺少观察时间；在途请求没有异常上限，最坏情况下由 4 个模型任务各 20 张图片形成约 80 个挂起调用。
- 容量边界决定采用每节点最高 10 RPM、全局最高 60 RPM、每节点紧急在途上限为 `有效 RPM × 10` 且最低 30；该上限只在节点连续约 10 分钟不返回时介入，不影响两分钟慢图继续按 RPM 放行。
- 连续 429 不能只做逐级缓降：第一次按档位降级并遵循 Retry-After，稳定窗口内第二次 429 必须直接降为 1 RPM 并熔断 15 分钟，避免多个已在途请求连续打穿上游。
- 容量仿真结果：3 RPM、60/120/600秒响应分别需要3/6/30个在途，均可保持3 RPM；1200秒响应才触发30上限并降为约1.5 RPM。10 RPM、60/120/480秒响应需要10/20/80个在途，可保持10 RPM；600秒响应需要100个，受全局80保护后约为8 RPM。
- 旧的 `provider_image_single` 会在一次调度放行内部依次尝试多个 `_image_models`，单节点还可能在 `provider_post` 内部重试 5xx；两条路径都会令真实图片请求数高于调度器统计 RPM。最终实现已改成“一次许可、一个优先模型、一次 HTTP”，任何重试都必须重新进入节点 RPM 队列。
- 档位失败必须记录“请求发出时的RPM”，不能只读取429返回时节点的当前RPM：多张8 RPM在途请求可能在第一次降到5后继续返回429。调度许可现携带 `attempted_rpm`，因此这些延迟响应仍会正确计入8 RPM失败记忆。
- 失败记忆采用进程内单调时钟，天然按节点隔离并在后台重启时清除；第三次封顶不会写入统计文件。普通成功不会清除6小时失败历史，只有时间窗口自然过期或用户手动解除。
- 用户截图中的第一组套餐公开档位为40/50/60/70/90 RPM，对应600/800/1100/1200/1600 RPD；第二组为90/100/110 RPM，对应1500/1700/1800 RPD。TPM均未限制。
- 现有代码把单节点RPM硬限制为10、全局限制为60，前端也只有3/5/8/10四档，确实严重低估了这些中转站的分钟吞吐；但RPD比RPM更紧，若以110 RPM持续发送，1800次日额度约16.4分钟即可耗尽。
- 当前最多4个模型任务、每任务最多约20张图，常规图片调用自然在途约80。以历史平均69.6秒计算，80在途的长期完成能力约69张/分钟，因此提高RPM主要缩短一批任务的“请求发出时间”，长期吞吐仍会受响应耗时和80在途限制。
- 截图没有标明具体域名与套餐分组，不能安全地自动把90/110 RPM写入某个现有API节点；控制台应提供截图档位预设和完全手动数值，让用户按真实Key所属分组选择。
- 用户希望把自定义参考的“风格图 + 人物 manifest + 人物图片”架构扩展到标准制作，但人物图片改为按画风隔离、可审核复用的角色设定图资产；任务开始前由 AI 将剧情角色槽位匹配到资产，并赋予本任务中的剧情名称。
- 目标数据流不是把全部角色图发给全部分镜，而是文案规划输出稳定 `role_id` 和每幕 `cast_ids`，后端据此只上传当前分镜实际出现的角色资产。
- 当前自定义参考会给每一张图上传完整参考列表，最多为 1 张风格图 + 15 张人物图；现有图片节点没有声明最大输入图片数，也没有按当前分镜筛选人物。
- 当前参考图职责依赖输入顺序与提示词编号进行软隔离；长提示词超过1000字符后机械保留前830和末160字符，角色多时存在截掉当前分镜或画风约束的风险。
- 标准画风并非全部拥有可发送的风格参考图；新架构必须给每个画风显式声明 `style_reference_mode`，区分不发送参考图、发送固定参考图、按分镜选择参考图，不能把前端风格预览图默认当作生图参考。
- 为兼容常见的4图输入节点，建议将每名角色的多角度素材预先合成为一张角色设定板，并让单次分镜请求默认使用 1 张风格图 + 最多 3 张当前角色设定图；超过上限必须有明确的拆分或合并策略。
- 用户采纳此前全部默认建议，并明确抽卡与当前任务解耦：无匹配资产时才从任务进入抽卡流程；角色经人工审核并保存到当前画风的公共资产库后，再返回原任务继续AI匹配和原生成流程。
- 用户授权真实图片接口能力检测与后续重构操作；检测应逐节点对 `/images/edits` 使用递增参考图数量做真实响应验证，结果保存为节点能力而非全局常量。
- 2026-09-08真实多图编辑探测结果：节点1 `aiapi.317ak.com` 精确上限16张；节点2 `api.mikoto.vip` 精确上限16张；节点3 `api.qiuqiutoken.net` 精确上限15张；节点4 `api.forkc2p.com` 精确上限10张。各节点均以成功返回图像数据为通过标准，后一张在400数量错误处收敛。
- 角色一致性的关键不是单独一句“延续上一分镜”，而是任务冻结角色设定图、规划阶段输出稳定 `cast_ids`、每张图片只上传实际出镜角色，以及同任务节点亲和四层共同约束；纯文本无法跨彼此独立的图片请求提供可靠视觉记忆。
- 图片提示词存在1000字符保护时，应把当前分镜放在输入图清单和风格长配方之前；角色说明压缩为逐图定义加一条共享身份规则，才能在8个角色参考等极端输入下仍保留剧情。
- 故障转移后的角色图仍可维持身份锚点，但不同中转实现可能造成笔触和采样差异；因此每张图必须审计最终节点、模型和参考图清单，亲和节点只在429、5xx或冷却时切换。
- 节点能力不能替代逐分镜筛选：即使节点允许10～16张，也只应发送当前 `cast_ids` 所需角色；上限用于调度兼容性与异常保护，不作为填满参考图的目标。
- 2026-09-08 缺失角色页面显示“我”并不是资产改名失败：角色卡标题读取任务级 `story_name`，公共资产改名修改的是 `asset.label`，两者职责不同；真正关联字段是唯一 `asset_id`。当前匹配响应没有返回 `asset_label`，所以前端无法同时展示“我 → 青年女”。
- 当前同画风资产允许重复 `label`，技术上不会串人，因为匹配、冻结和生图全部依赖12位 `asset_id`；但重复名称会造成审核者混淆，应在创建和改名时自动追加序号，并继续以ID作为唯一键。
- 当前缺失卡片末尾的“需抽卡”是普通 `<em>`，没有点击事件；审核通过也只刷新公共资产列表，不更新 `characterBindings`，因此任务区不会自动从红色变绿，必须人工重新运行AI匹配。
- 角色抽卡并非全部上传画风图：`style_only_reference_context()` 只为清透日系生活绘本、漫画墨线解释风和纸感隐喻拼贴风返回内置风格图；3D黏土及其余普通画风只使用文字视觉配方。
- 本轮确定日系正式分镜取消内置画风图，但角色抽卡继续使用风格图；正式分镜仍按 `cast_ids` 上传人物设定图，因此“无画风图”不等于“无任何输入图”。
- 现有单图重生成分支单独复制了风格图路由，却没有重新加入标准制作的逐分镜人物参考；重构提示词组装时应与正常生产共用同一 board spec 构建函数。
- 取消画风参考说明不会取消图片中转站的1000字符限制；只是减少提示词与一张输入图，常见1～3人物分镜更不容易触发压缩。
