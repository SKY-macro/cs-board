# 执行进度

- 2026-09-06：开始研究并集成 story-to-handdrawn-video 的 20 种画面风格。
- 已确认实际 Git 仓库为 E:/cs-board，远端 fork 指向 SKY-macro/cs-board。
- 网页读取指定仓库失败，切换为 Git 只读克隆方式。
- 已完整读取来源 Skill、20项机器可读风格库及 MIT 许可，并确定采用“保留现有风格 + 追加20种”的兼容方案。
- 已复制机器可读风格库和许可文件；首次复制 PNG 因 LiteralPath 不展开通配符而失败，尚未复制图片。
- 已校验并复制20张来源 PNG；后端追加20种完整风格配方，前端追加20张带来源原图的风格卡片，并添加基础测试。
- 已补齐彩铅日记的详细 profile 和两份 MIT 许可；38项后端测试、2项前端测试、生产构建及 HTTP 检查全部通过。
- 检查期间另一个已授权改动提交为 e5f706b；本次风格改动与其已正确分离。
- 已提交 `c9c46fb` 并成功推送至 `fork/main`。
- 2026-09-06：继续修复通用 503 换模，以及文本/图片接口采用响应模型结果的问题。
- 已实现通用 429/500/502/503/504 自动换模；文本与图片分别使用各自 `/models` 响应候选，并记录成功响应的实际模型名。
- 新增3项回归测试，全套 Python 41项及前端2项测试通过。
- 修复提交 `a3e76ee` 已推送；后台已用项目虚拟环境重启，真实任务确认发生候选模型自动切换。
- 2026-09-06：用户选择“自动读取 `/models`，只选择一个优先模型”，开始收敛候选队列。
- 已清除历史多模型候选，并让文本、图片各自只保留 `/models` 响应中选出的一个优先模型；全量测试通过。
- 单模型机制提交 `e6a97b3` 已推送；后台重启完成，健康接口返回正常。
- 2026-09-06：开始从源头过滤不可调用模型；确认中转站 `/models` 不提供健康状态元数据。
- 已实现最小真实请求探测：按优先顺序跳过429/503，首个成功即停止并作为下拉框唯一文本模型；全量测试通过。
- 修复提交 `5f10207` 已推送并重启后台；真实识别接口最终只返回可调用文本模型 `gpt-5.6-luna` 和图片模型 `gpt-image-2`。
- 2026-09-06：开始参考用户提供的 53 秒竖屏视频及 story-to-handdrawn-video，新增“黑白线稿逐渐上色 + 手写体字幕”动画呈现方式。
- 已抽取参考视频九宫格关键帧：确认其核心呈现为白底手绘插图、顶部/留白区手写中文，以及黑白线条基础上的克制局部上色。
- 新增动画模式的后端全量测试 55/55 通过；首次前端测试误在仓库根目录执行，已切换至 web/ 目录重跑。
- 前端测试 2/2 与生产构建通过；真实 4 秒预览确认字幕准确、不压人物，并呈现“文字 → 黑白线稿 → 彩色画面”。
- 用户指出新模式仍残留白板式逐笔揭示，要求与白板模式完全切割并复刻演示视频；开始重新逐帧分析约每 4.4 秒一次的场景节奏。
- 逐帧确认演示视频使用平面图层水平擦入：文字 0–22%、黑白层 18–58%、彩色层 52–88%，其余时间保持完整页面。
- 已新增独立 `render_story_color.py`；绘本模式不再调用骨架追踪、笔迹路径、笔尖或画手逻辑，并强制一幕一页与原生目标分辨率。
- 回归结果：Python 55/55、前端 2/2、生产构建成功；1080×1440、30fps、4.4秒正式尺寸预览生成成功。
- 2026-09-07：开始将用户上传的生活绘本参考图提炼为独立画面风格；采用“技法复刻、内容隔离”，不继承示例人物与场景。
- 已新增“清透日系生活绘本”风格卡、完整后端视觉配方及用户参考预览图；Python 56/56、前端 2/2 和生产构建全部通过。
- 新风格真实 A/B 冒烟：整图参考会复制原场景，纯提示词能按新剧情生成且视觉明显贴近目标；最终选择无内容污染的收紧提示词方案。
- 已移除全部持久化“新增”角标并加入防回归断言；前端 3/3 测试及生产构建通过。
- 已完成“清透日系生活绘本”视觉重构：只锁定造型与媒介，不锁定人物位置；同时修复长配方截掉分镜内容的问题，最终真实样张和全量回归通过。
- 2026-09-07：API 设置已从主页面内嵌区改为遮罩模态窗口；停止输入 900ms 后自动保存，页面内显示等待、保存中、已保存和失败状态，不再弹出浏览器确认框。
- 文本和图片中转站均支持保存后按当前变更节点自动识别模型并更正下拉框；签名去重避免模型回写形成重复探测。前端 5/5 测试和生产构建通过，真实界面确认遮罩、内部滚动、固定操作区和页面内保存提示正常。
- 2026-09-07：修复新增文本/图片中转站继承旧接口地址的问题；新节点现在以空网址和空 Key 创建。新增回归断言后前端 6/6 测试与生产构建通过，实际界面新增验证确认两类节点均为空白，并已删除测试节点恢复原配置。
- 2026-09-07：重构文本、图片生成和参考图编辑的中转站切换逻辑。进度使用原始节点编号与配置总数，明确显示可调用数量及被跳过节点的原因；多节点时每站只尝试一次便快速切换，单节点仍保留三次瞬时故障重试；全部失败会汇总每个节点的错误。Python 61/61、前端 7/7 与生产构建通过。
- 2026-09-07：开始新增任务级身份策略选择，目标为“身份一致／默认男主角／默认女主角”三选一，默认身份一致，并贯穿分镜规划、图片提示词、任务保存和历史参数恢复。
- 已先添加身份模式互斥及默认持久化失败测试；失败原因与预期一致：后端尚未接受 `identity_mode`，任务记录也没有该字段。
- 已完成任务级人物身份三选一：默认“身份一致”，可切换固定中国青年男主角或女主角；选择会随任务持久化、历史参数恢复，并贯穿分镜规划与最终生图提示词。
- 浏览器实测覆盖默认、男主角、女主角切换及自定义参考模式；三种标准模式文案随选择正确替换，自定义参考模式隐藏该选择并继续以上传人物参考为准。
- 最终验证：Python 69/69、前端 9/9、生产构建与 `git diff --check` 通过；最长实际图片提示词 997 字符，身份与分镜内容均完整。
- 后端已用更新后的代码重启，`/api/health` 返回正常，模型队列并发与渲染服务状态正常。
- 2026-09-07：开始重构任务详情图片交互与提示词布局；目标为单击切换提示词、双击或四角图标放大、提示词固定内嵌右栏，并将详情页最小字号提高到“生成图片”标题级别。
- 已完成代码重构：图片卡单击只选择提示词，双击与右上角四角按钮打开大图；提示词编辑器已移动到参数区下方，取消独立固定定位。新增前端断言后 10/10 测试与生产构建通过。
- 真实桌面交互已覆盖单击、双击和四角放大按钮三条路径；15px 字号下限、选中边框、右栏嵌入、长提示词滚动及弹窗横向适配均通过检查。
- 最终回归完成：Python 69/69、前端 10/10 与生产构建全部通过；开发页面已通过热更新加载新版界面。
- 2026-09-07：开始把独立悬浮的“本次使用的素材”面板迁入任务详情右栏，保留音频播放、参考图放大和用当前图片重渲染功能，并消除其覆盖图片画廊的问题。
- 用户补充要求：单图重新生成后，进度区在图片查看按钮下直接给出同尺寸绿色“重新渲染成片”，并隐藏尚未按新图合成的旧视频。
- 已完成两项重构：素材面板迁入任务详情右栏；单图更新完成后写入明确的 `needs_rerender` 状态，进度区显示同尺寸绿色重渲染按钮，并在进度、历史和详情三处隐藏旧成片。
- 最终验证：Python 69/69、前端 12/12、生产构建及 `git diff --check` 通过；真实历史任务确认素材区内嵌、旧浮层消失、旧视频/下载入口隐藏，15px 字号下限保持不变。
- 2026-09-07：开始把图片“每任务3路”改造成跨任务共享的节点级自适应并发，并开放本机多任务流水线。
- 已实现图片节点统计与持久化：默认3路；429按 `Retry-After` 冷却并降为1路；连续成功按1→2→3恢复；503保持临时故障语义并切换备用节点。
- 已把本地渲染限制为最多2个视频任务并行，文案队列保持4任务；多个视频可分别处于文案、生图、渲染阶段，待处理任务安全上限仍为20。
- 运行中进度区在“取消任务”下方新增绿色“创建新的任务”；点击只清空任务内容和素材，保留画风及成片设置，旧任务继续由历史轮询在后台更新。
- 最终回归：Python 71/71、前端14/14、生产构建和 `git diff --check` 通过；真实页面确认API设置显示“图片单节点默认3路自适应、本地渲染2个任务”。未提交真实付费生图任务，避免为了界面测试产生额外API费用。
- 2026-09-07：开始把 v9 固定 3 RPM 扩展为完整的多节点自适应调度。已确认当前真实运行中单节点存在 5 个在途请求，说明“返回不阻塞后续提交”已经生效；本轮重点补齐主动多节点分流、最高 10 RPM、连续 429 直降 1 RPM、熔断和异常在途保护。
- 已完成详细调度设计文档 `docs/image-rpm-scheduler.md`：确定每节点 1/3/5/8/10 档、默认最高 10 RPM、全局最高 60 RPM、每节点至少 30 个在途保护、连续第二次 429 直降 1 RPM 并熔断 15 分钟。
- 已添加4项后端失败测试并确认全部按预期失败：旧类没有多节点主动选择、升档观察参数和在途保护；首次429也会错误地直接降到1 RPM。失败证据已锁定本轮实现边界。
- 已完成调度器主体与前端配置初版：节点支持最高RPM 3/5/8/10，调度器使用FIFO等待、跨节点最早可用选择、连续429/5xx熔断和在途保护；首次前端回归仅因测试字段正则过时失败，构建成功，已修正断言。
- 调度器主体与设置界面已完成；后端78项测试、前端14项测试和生产构建通过。容量仿真确认正常60–120秒慢图不会触发保护，极端10–20分钟无返回时才主动限制在途增长。
- 最终审查封堵了 RPM 旁路：一次调度许可现在严格只发出一次图片 HTTP 请求，`/models` 只确定一个优先图片模型，内部模型轮询与图片 HTTP 自动重试均不再绕过调度器。新增 FIFO 公平性测试后，Python 79/79、前端 14/14 与生产构建通过。
- 活跃任务归零后已安全重启后台；健康接口确认加载 `narrated_deck_v10_adaptive_rpm`，初始 3 RPM、单节点最高 10 RPM、全局最高 60 RPM、全局在途上限 80，前端 HTTP 200。
- 2026-09-07：新增节点级高档失败记忆。8/10 RPM 首次429锁定运行上限5 RPM 30分钟，6小时内第二次锁6小时，第三次在当前后台进程内封顶5 RPM；降档恢复改为30分钟且100张成功，所有记录按节点隔离。
- API 设置状态增加当前/运行/配置三层RPM、锁定倒计时、恢复观察与“解除档位记忆”操作；手动解除和后台重启均可清除运行时永久封顶，但不会绕过仍生效的429冷却。
- 回归完成：Python 86/86、前端14/14和生产构建通过；覆盖6小时窗口过期、单节点隔离、30分钟/100张恢复门槛、第三次运行时封顶、重启清除及手动单节点解除。
- 后台已安全切换至 `narrated_deck_v11_rpm_tier_memory`；真实健康接口显示当前节点3 RPM、运行上限10、配置上限10、失败记忆0，前端HTTP 200。
- 2026-09-07：开始根据两个中转站公开的40–110 RPM、600–1800 RPD重新设计容量系统。已确认旧10 RPM硬上限需要移除，同时必须把日请求预算与分钟限速分开控制，避免高RPM在十几分钟内耗尽全天额度。
- 容量模型已实现：单节点标称RPM上限300、全局500；默认安全率80%，实际发送目标和日安全预算同时乘安全率。RPD按请求发出计数并跨重启持久化，本机自然日归零；单节点额度耗尽自动分流，全部耗尽立即报错而非空转。
- API设置已升级为图片容量控制台，内置截图中的8个RPM/RPD套餐预设，也允许手动输入标称RPM/RPD、安全使用率、全局RPM与全局在途上限；修改随原有自动保存机制生效。
- 控制台实时状态已补齐今日请求/日预算、动态失败封顶值和全局在途信息；不再把高RPM节点错误显示为固定封顶5 RPM。
- 容量仿真确认：90/1600与110/1800套餐按80%运行时合计160 RPM、2720 RPD；在69.6秒平均耗时和80在途条件下稳定完成约69张/分钟，RPD仍是连续生产的主要硬边界。
- 全量回归完成：项目虚拟环境Python 92/92、前端14/14、生产构建和`git diff --check`通过；等待安全重启与GitHub提交。
- 修复旧统计迁移显示：健康接口会按当前配置同步全部图片节点，缺失RPM字段的旧节点保守按10 RPM迁移，不再误显示为300 RPM硬上限。
- 后台已安全重启到`narrated_deck_v12_capacity_console`；健康接口显示3个节点全部加载，现有旧节点按10 RPM标称、80%安全率运行在8 RPM，前端HTTP 200。最终Python 93/93、前端14/14通过。
- 2026-09-08：开始规划“标准制作角色资产库与逐分镜一致性”重构。已确认目标为按画风维护可审核角色设定图，任务级 AI 进行剧情角色映射，并按每幕实际 `cast_ids` 选择参考图；当前处于关键产品与数据契约确认阶段，尚未修改生产代码。
- 用户确认采纳全部建议，并授权实测图片接口参考图上限；补充确定抽卡为独立公共资产库，任务缺少匹配角色时进入抽卡、审核入库后返回任务继续执行。
- 已完成四个图片节点真实 `/images/edits` 多图能力探测：精确上限依次为16、16、15、10张；开始据此设计节点能力持久化和逐分镜参考图选择。
- 已完成独立公共角色抽卡库：按画风生成四视图角色设定图、人工审核入库、停用/恢复和删除；标准制作可让 AI 提取角色并匹配，缺失时自动带要求进入抽卡区，审核后重新匹配。
- 已完成任务角色冻结、稳定 role_id/cast_ids、逐分镜参考图筛选、节点容量过滤和同任务节点亲和；每张图在任务状态中记录实际节点、模型及参考图，故障时才切换兼容节点。
- 提示词已改成“当前分镜优先→输入图单一职责→身份/画风规则”，新增长参考清单压缩回归，保证1000字保护下仍保留剧情。抽卡线程若被后台重启中断会转成可处理错误，不自动重复付费调用。最终 Python 102/102、前端16/16及生产构建通过；浏览器已验证角色抽卡弹窗、自动缺失角色带入、API容量显示及中断恢复状态。
- 2026-09-08：开始修复缺失角色抽卡交互。已确认“我”是剧情称呼、青年女是公共资产名，绑定实际使用 asset_id；根因是匹配响应未传资产显示名、缺失状态没有按钮、审核后没有回填当前任务。
- 已完成双层命名与去重：任务继续保留“我/他/老板”等剧情称呼，界面另显“已使用资产：青年女”等公共资产名；同画风重名自动保存为“名称 2/3…”，身份始终由唯一 asset_id 绑定。
- 缺失角色卡已改为“一键去抽卡”：点击后直接携带该角色描述提交单张候选，审核通过即回填原角色并由红卡转绿；失败、拒绝或删除后会恢复按钮，允许再次抽卡。
- 最终回归：Python unittest 113/113、前端17/17、生产构建及 `git diff --check` 通过；运行版本为 `narrated_deck_v21_character_draw_handoff`。浏览器实测“我”仍为剧情名，同时正确显示“已使用资产：青年女”，三名角色均自动点亮为已匹配。为避免额外费用，没有在验收时点击真实抽卡请求。
- 已将“本任务角色资产”整体移到右侧栏“成片设置”之前；前端17/17与生产构建通过，浏览器DOM实测右栏顺序为“本任务角色资产 → 成片设置”。
- 2026-09-08：角色匹配升级为固定性格脸相机制。文本模型必须阅读全文并以最终揭示为准，分别输出核心性格、固定脸相与临时行为；资产描述中的性格气质继续作为永久设定，而不是被删除。
- 后端新增人格兼容与0.78置信度闸门；只符合年龄性别或库中仅有一个同类候选不再构成匹配。核心性格和固定脸相随任务冻结，并进入逐分镜角色参考说明；缺失角色的一键抽卡同时携带这些字段。
- 全量回归为Python 115/115、前端17/17、生产构建与diff检查通过。安全升级至 `narrated_deck_v22_persona_matching` 后，使用用户原文真实分析：女主以0.90匹配“青年女”；男主因旧资产“精明算计脸”与结尾揭示的“体贴、克制付出”冲突而返回null，正确进入抽卡。
- 2026-09-08：开始把清透日系生活绘本正式分镜改为纯视觉配方。用户确定只取消正式分镜画风图、角色抽卡仍使用风格图；旧角色资产批量重生成，背景保持线稿无填色，仅允许剧情必要小面积语义色，保留轻纸粒并禁止水彩晕染和泛黄。
- 已添加5项定向失败测试：确认旧代码仍给正式分镜/预览注入内置画风图，且缺少生产参考策略、统一board spec与配方版本函数；失败结果符合预期。
- 已实现统一 `build_board_generation_spec()`：正常批量生成与单图重生成都重新按 `cast_ids` 选择角色图，并显式区分“画风参考图”和“人物参考图”。真实任务数据确认截图中的第四幕含两个正确角色ID，旧分支遗漏图片是人物漂移根因。
- 日系正式分镜已改为纯文字视觉配方，角色抽卡仍保留内置风格图；配方锁定背景线稿不填色、剧情必要小面积语义色、细微纸粒、禁止水彩晕染与泛黄，并把内容/风格改成必须同时满足的并列硬约束。
- 已增加 `clear-storybook-text-v2` 配方版本、历史未编辑提示词自动升级、人工提示词优先、重新生成参考图审计和旧角色资产安全归档/批量重建入口；11项定向测试通过。
- 首轮全量回归前端19/19通过；Python 123项中仅自定义参考的旧直调方式缺少新增风格图标记，已补充兼容识别后继续回归。
- 最终全量回归通过：Python 123/123、前端19/19、生产构建与 `git diff --check` 均成功；后台已安全升级至 `narrated_deck_v23_storybook_text_recipe`，真实提示词预览为974字且不含画风参考说明或旧优先级句。
- 已用截图对应的两条真实任务验证第四幕：均解析出 `role_01`、`role_02` 并携带两张冻结角色设定图，修复前的单图重生成分支则为0张。两份旧日系角色资产已按新配方重建，旧图归档到各自 `revisions`，新图保持 `review` 等待人工审核。
- 2026-09-08：开始修复角色匹配与抽卡交互。用户报告模型因“同类候选只有一个”主动返回空绑定、资产库卡片不能作为新设定图参考、缺失角色按钮状态不直观，以及仍显示数字匹配度。
- 已先添加后端3项与前端3项失败断言，准确复现：来源资产字段未持久化、抽卡不上传来源角色图、唯一候选否决句仍存在、卡片无选择状态、审核入口被禁用和百分比仍显示。实现后后端定向3/3通过；前端仅剩一项测试变量名绑定过严，已改为同时验证选择状态传入函数及函数写入请求字段。
- 已修复匹配提示词：候选数量不再构成否决，唯一候选与多个候选统一执行性别、年龄、外观、人格及0.78置信度标准。公共资产卡可点击/键盘选中并预填名称描述；新抽卡请求冻结来源图副本，图片模型收到明确编号的人物身份参考，来源损坏时终止而不降级成人物重设计。
- 缺失角色按钮已改为“开始抽卡”；提交后保持可点击并显示“开始审核”，点击直接打开抽卡库。已绑定角色统一只显示“匹配”，不再展示内部置信度百分比。前端20/20及生产构建初步通过。
- 最终全量回归通过：Python 126/126、前端20/20、生产构建与 `git diff --check` 均成功；后台已安全升级至 `narrated_deck_v24_character_draw_source`。
- 使用真实“300元项链”文案与当前3D黏土库验证唯一候选：女友以0.97绑定“青年女”，男友以0.99绑定“青年男”，不再因同类候选只有一个而拒配。
- 浏览器实测角色卡单击选中、再次选择另一卡可切换，左侧名称和外观说明随之预填，橙色边框明确标出当前身份底图；为避免额外图片费用，验收未提交真实抽卡请求。
- 本轮实现已提交并推送 `fork/main`，功能提交为 `c9212c4`；提交时检测到2个新活跃任务，保留已运行的v24服务，没有为提交重复重启或中断用户任务。
- 2026-09-08：开始解释并统一画风模板预览与真实分镜提示词。已确认两者差异来自角色资产上下文：模板无任务角色图，真实分镜按 `cast_ids` 上传冻结角色图并自动替换通用身份文字。
- 已按追加要求把标准画风模板改成角色资产版结构：默认身份一致时展示占位的“输入图1定义人物”、详细外观、核心性格、固定脸相和人物图锁定规则；真实任务仍按本幕角色动态替换。
- 已删除默认身份一致中的三句旧长段落，仅保留防止随镜头重新设计角色的最小约束；定向后端3/3、前端20/20及生产构建通过。
- 首次全量Python回归127项中1项失败：日系绘本模板加入人物参考占位后超过1000字符并触发截断；前端生产构建成功。失败边界已确认，改为压缩占位参考规则而不放宽真实接口上限。
- 精简后日系最长模板为1000/1000字符且不截断，人物占位说明完整保留输入图身份、外貌锚点和状态随分镜三层规则；最终Python127/127、前端20/20和生产构建通过。
- 浏览器实际切换到“清透日系生活绘本”后，画风区显示“系统图片提示词模板（含人物参考规则）”、`输入图1定义人物`、人物锁定规则及“未截断”，旧跨分镜长段落未出现。
